-- Raster: RS_* functions (CereusDB full). Load GeoTIFF/TIFF via registerGeoTIFF / registerFile first.
-- Example: SELECT RS_Width(rast), RS_Height(rast) FROM your_table;

SELECT 1 AS see_comments_above;
