# LCOV from Vitest runs in packages/core with paths like SF:src/...
fixes:
  - "src/::packages/core/src/"

# Patch status is off until overall coverage is higher; re-enable in codecov.yml when ready.
coverage:
  status:
    patch: off
